# 使用指南（个人版魔改 GitLens）

写给自己看的。3 分钟就会用。

---

## 🎯 这插件干嘛的

让你在 VSCode 里看 Git 信息，不用开 GitHub 网页，也不用敲命令行。

**你用得最多的 3 件事：**

1. 看某行代码谁改的、啥时候改的
2. 看项目的提交历史、点开看某次改了啥
3. 看分支、切换分支、拉取推送

---

## ⚡ 5 分钟上手

### 1️⃣ 看当前行 Blame

光标停在任意代码行 → **行尾会自动出现灰字**：`作者 · 多久前 · commit 消息`。

鼠标悬停在灰字上 → 弹一个详情卡片（头像、完整 message、点链接跳 GitHub）。

### 2️⃣ 看项目的提交历史

左边活动栏 → 找 **GitLens 图标**（蓝色猫图标） → 点开。

会看到一列视图，最重要的叫 **提交（Commits）**，默认已展开。

列表里每条 commit 显示：
- 左边：作者头像（Gravatar）
- 标题：commit 消息
- 副标题：`作者 · 时间 · +新增文件~修改文件-删除文件`

### 3️⃣ 看某个 commit 改了啥（重点！）

**左键单击** Commits 列表里任意一条 commit → 右侧弹出 **详情页**。

详情页里有：
- 大头像 + 作者 + 时间 + SHA
- **文件列表**（每个文件显示新增/修改/删除 + 行数统计）
- 点击文件行 → 打开新旧代码对比

顶部三个按钮：
- `← 上一条` / `下一条 →`：在历史里前后切换
- `↻ 刷新`：重新加载

---

## 💡 日常常用场景

### "我想看当前这个文件的完整修改历史"

左侧 GitLens → **文件历史** 视图（默认展开）。自动跟随你当前打开的文件。

### "我想切换到别的分支"

左侧 GitLens → **分支** 视图 → 右键某个分支 → `Checkout`。

或者：`Ctrl+Shift+P` → 输入 `gitlens switch` → 选分支。

### "我想 pull/push"

- VSCode 左下角状态栏有个同步图标，点一下就行
- 或者侧栏顶部有 ⬇️ 和 ⬆️ 图标

### "我想看谁提交最多"

左侧 GitLens → **贡献者** 视图（默认展开）。按提交次数排序。

### "我想复制某个 commit 的 hash"

详情页里有 `[复制]` 按钮，一键复制完整 SHA。
或者详情页右侧 `[在远程打开 ↗]`，直接跳 GitHub。

---

## 🗂 侧栏视图都是啥

默认已经精简过，按默认可见性排：

**默认展开（每天用）：**
- **提交 (Commits)**：当前分支的提交历史 —— 最常用
- **文件历史 (File History)**：当前打开文件的历史
- **贡献者 (Contributors)**：谁提交过

**默认折叠（偶尔用）：**
- **分支 (Branches)**：所有本地+远程分支

**默认隐藏（基本不用，右上三点菜单里手动开）：**
- 仓库 / 远程 / 标签 / 暂存 / 搜索比较 / 行历史

已删除的：~~工作树 (Worktrees)~~、~~提交图 (Commit Graph)~~

---

## 🎹 一些值得记的操作

| 操作 | 怎么做 |
|------|--------|
| 命令面板 | `Ctrl+Shift+P` → 输 `gitlens` |
| 切换行内 blame 开关 | `Ctrl+Shift+P` → `GitLens: Toggle Line Blame` |
| 切换文件整页 blame | `Ctrl+Shift+P` → `GitLens: Toggle File Blame` |
| 打开设置页 | `Ctrl+Shift+P` → `GitLens: Open Settings` |

---

## 🔧 想再调整？

### 觉得行尾灰字太吵
```
设置搜索: gitlens.currentLine.enabled → 关掉
```

### 觉得文件 blame 注释条太花哨
```
设置搜索: gitlens.blame.format → 改短一点
```

### 觉得头像加载慢 / 不显示
头像默认走 `cravatar.cn`（国内 Gravatar 镜像）。如果公司网络屏蔽，可以再改 `src/avatars.ts` 最后那行 URL。

### 提交列表想看行数（+X -Y）不是文件数（+X~Y-Z）
设置里找 `gitlens.views.formats.commits.description`，把 `changesShort` 改成 `changes`。

---

## ❓ 出问题了

### 点 commit 不弹详情页？
- 装新版后要**彻底关闭 VSCode**再打开（不是 Reload Window）
- 检查：`Ctrl+Shift+P` 搜 `显示提交详情`，能搜到说明命令已注册

### 详情页空白？
- 打开开发者工具（帮助 → 切换开发人员工具）看 Console 红字
- 截图给我改

### 侧栏视图消失了？
- 右键侧栏标题栏 → 勾选需要的视图

---

## 📦 版本说明

这是 v12.0.1（最后一个免费开源版）的个人魔改版：

- ✅ 全中文汉化
- ✅ 重做 commit 详情页（GitHub 风格）
- ✅ 单击 commit 直接弹详情
- ✅ 头像换国内镜像
- ✅ 侧栏默认清爽
- ❌ 删除 Commit Graph（个人觉得不好用）
- ❌ 删除 Worktrees（个人用不到）
- ❌ 删除 Premium 订阅相关

魔改说明：这不是官方版，是基于 v12.0.1 开源代码的个人定制版。官方后续版本转为闭源商业化。
